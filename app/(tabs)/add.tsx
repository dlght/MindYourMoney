import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function AddScreen() {
  return (
    <ScreenPlaceholder
      title="Add"
      description="Adding an expense lands here in a later feature (F2)."
    />
  );
}
