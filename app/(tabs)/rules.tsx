import { ScreenPlaceholder } from "@/components/ScreenPlaceholder";

export default function RulesScreen() {
  return (
    <ScreenPlaceholder
      title="Rules"
      description="The notification rule editor lands here in a later feature (F4)."
    />
  );
}
